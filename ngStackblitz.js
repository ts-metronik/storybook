// `ng` script wouldn't execute in the Stackblitz Terminal, so this bypasses the checks
// done by `ng.js` and just initializes the cli directly.
require('./node_modules/@angular/cli/lib/init.js');
